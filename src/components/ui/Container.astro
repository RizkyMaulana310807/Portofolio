---
interface Props {
  class?: string;
  id?: string;
}

const { id, class: className = "" } = Astro.props;
---

<div id={id} class={`mx-auto w-full max-w-7xl px-4 transition-colors duration-300 sm:px-6 lg:px-8 ${className}`}>
  <slot />
</div>