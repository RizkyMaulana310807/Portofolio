---
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<div
  class={`rounded-2xl border border-neutral-200 bg-white p-6 shadow-sm ${className}`}
>
  <slot />
</div>